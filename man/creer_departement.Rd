% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/creer_departement.R, R/summary_departement.R
\name{creer_departement}
\alias{creer_departement}
\title{Créer un objet de type "departement"}
\usage{
creer_departement(df)

creer_departement(df)
}
\arguments{
\item{df}{Un dataframe contenant au moins la colonne suivante :
\itemize{
\item \code{Code.du.département} (character ou numeric) : Le code identifiant le département.
}}
}
\value{
Un dataframe de classe \code{"departement"}, qui peut être utilisé avec des fonctions spécifiques aux départements.

Un dataframe de classe \code{"departement"}, prêt à être utilisé dans d'autres fonctions spécifiques aux départements.
}
\description{
Cette fonction transforme un dataframe représentant un département en un objet de classe "departement".

La fonction vérifie que le dataframe ne contient qu'un seul département distinct (basé sur \code{Code.du.département}),
et lui attribue la classe \code{"departement"} s'il ne l'a pas déjà.
}
\details{
\itemize{
\item La fonction vérifie que la colonne \code{Code.du.département} est présente dans \code{df}.
\item Elle s'assure que le dataframe représente un seul département, sinon elle génère une erreur.
\item La classe \code{"departement"} est ajoutée à l'objet pour permettre un traitement spécifique.
}

Cette fonction transforme un dataframe représentant un seul département en un objet de classe "departement".
\itemize{
\item La fonction compte les valeurs uniques dans la colonne \code{Code.du.département} pour s'assurer qu'il n'y a \strong{qu'un seul département}.
\item Si plusieurs départements sont détectés, la fonction s'arrête avec un message d'erreur.
\item Si la classe \code{"departement"} n'est pas déjà présente, elle est ajoutée à l'objet.
}
}
\examples{
\dontrun{
data <- data.frame(
  Code.du.département = rep("75", 10),
  Nom.du.département = rep("Paris", 10),
  Population = c(100, 200, 150, 180, 220, 250, 300, 270, 260, 280)
)
departement <- creer_departement(data)
class(departement)  # Vérifier la classe
}

\dontrun{
# Exemple de dataframe valide avec un seul département
data <- data.frame(
  Code.du.département = rep("75", 5),  # Paris
  Nom.du.département = rep("Paris", 5),
  Population = c(100, 200, 150, 180, 220)
)

# Créer l'objet departement
departement <- creer_departement(data)
class(departement)  # Vérifier la classe
}

}
