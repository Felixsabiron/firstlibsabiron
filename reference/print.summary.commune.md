# Affichage du resume pour summary.commune

Cette fonction definit l'affichage personnalise pour les objets de
classe `summary.commune`.

## Usage

``` r
# S3 method for class 'summary.commune'
print(x, ...)
```

## Arguments

- x:

  Un objet de classe `summary.commune`.

- ...:

  Arguments supplementaires (non utilises).

## Value

NULL (resume affiche dans la console)
