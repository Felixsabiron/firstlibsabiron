# Methode S3 pour summary.departement

Cette fonction redirige automatiquement
[`summary()`](https://rdrr.io/r/base/summary.html) vers
[`summary_departement()`](https://felixsabiron.github.io/firstlibsabiron/reference/summary_departement.md)
pour les objets de classe `departement`.

## Usage

``` r
# S3 method for class 'departement'
summary(object, ...)
```

## Arguments

- object:

  Un objet de classe `departement`.

- ...:

  Arguments supplementaires (non utilises).
